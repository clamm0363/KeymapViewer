# 実装計画

## 【計画】ガイドモーダル更新とページネーション追加（2026-06-05）

### 作業ブランチ
- `dev`

### 目的
- 「How to use / 使い方ガイド」の日英内容を、現在の実装機能に合うように最新化する
- 長文化したガイドを読みやすくするため、モーダルヘッダーにページネーションを追加する
- 既存の言語切替と共存しつつ、ガイド本文をページ単位で整理する

### 現状整理
- `js/components/Modals/HelpModal.js`
  - 日本語・英語ともに単一ページ構成で、最近追加された表示設定、CALLOUT、キー編集、SVGアイコン置換などの説明が不足している
- ヘッダーには言語切替だけがあり、長い本文を章ごとに移動する導線がない
- 現状の本文データはフラットな key 群で持っており、ページ分割や差し替えの保守性が低い

### 実装方針
- ガイド本文を「ページ -> セクション -> 項目」の構造へ整理し、日英両方で同じページ数・同じ章立てを保つ
- ページネーションはタイトル右に置き、前後移動と現在ページ表示をコンパクトにまとめる
- 内容更新では、現行UIに存在する表示設定、レイヤー操作、CALLOUT、キー編集、SVGアイコン上書き、共有/保存まわりを優先して反映する
- 既存の言語切替・閉じるボタン配置はできるだけ維持しつつ、ヘッダー情報量だけ増やす

### 実装ステップ
1. `HelpModal` の本文データをページ単位の構造へ再編する
2. 日本語・英語の両方で、最新機能を反映した説明文へ更新する
3. タイトル右にページネーションUIを追加する
4. ページ切替時に本文だけが差し替わる描画へ整理する
5. `node --check` と `npm run lint`、`npm run test` で確認する

### ファイル別の想定変更
- `js/components/Modals/HelpModal.js`
  - 本文データ構造
  - ページネーション state
  - ヘッダーUI
  - 最新機能説明の本文更新

### 設計上の確認ポイント
- ページ数は多すぎず、1ページの情報量も詰めすぎないこと
- 日英で説明対象のズレを作らないこと
- 将来の機能追加時に本文更新しやすいデータ構造になっていること

### 推奨する実装の切り方
- まず本文データの整理とページ区切りを入れる
- 次にヘッダーのページネーションと本文差し替えを追加する
- 最後に文面を磨き、用語の統一を確認する

### 検証方針
- 日本語・英語の両方でページ送りが機能すること
- タイトル右のページネーションがヘッダー内で崩れないこと
- 既存の言語切替と閉じる操作が維持されること
- ガイド本文に現行機能の説明漏れがないこと

### 実施メモ
- ガイド文面は UI のラベル名とそろえ、過度に抽象化しない

### 実施結果
- `HelpModal` の本文を、単一ページのフラット構成から「4ページ構成の章立て」へ再編した
- 日本語版・英語版の両方で、スロット管理、表示設定、CALLOUT、キー編集、SVG アイコン置換、共有/保存まわりまで含めて内容を更新した
- タイトル右に前後移動付きのページネーションを追加し、長い説明をページ単位で読めるようにした
- 既存の言語切替と閉じる操作は維持した
- `node --check js/components/Modals/HelpModal.js`
- `npm run lint`
- `npm run test`
