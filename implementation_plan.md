# 実装計画

## 目的

ブラウザ幅を狭めた場合や表示倍率を上げた場合でも、主要 UI が表示領域外へ押し出されにくいようにレイアウトを見直す。
今回の最優先は、各スロット右上のハンバーガーメニュー相当の `Display Settings` ボタンへ安定して到達できる状態を取り戻すことである。

## 作業ブランチ

- `fix/responsive-slot-header-access`

## 現状認識

- 以前は狭い表示域でも UI が段組みや折り返しで追従していたが、現在は一定幅以下でスロット上部の操作群が横に張り出しやすい
- 特にスロットヘッダは左側のタイトル群と右側の操作ボタン群を 1 行固定気味に保持しており、右上ボタンが表示領域外へ逃げる可能性がある
- アプリ全体コンテナにも `min-w-full w-fit` があり、内部コンテンツ幅に引っ張られて横方向へ広がる挙動が疑わしい

## 調査対象

- `js/app.js`
  - アプリ全体コンテナの幅制御
  - `STACK` / `GRID` 配置時の親レイアウト制約
- `js/components/DeviceSlot.js`
  - スロットヘッダの `flex` 構成
  - タイトル領域と右上操作群の縮み方
  - 設定パネル展開時の折り返しと余白
- 必要に応じて関連スタイル
  - `Header.js`
  - `Keyboard.js`
  - キーボード描画を包むラッパーの `overflow` / `min-width`

## 原因仮説

1. アプリ最上位の `w-fit` が、狭い viewport でも内容幅優先のレイアウトを誘発している
2. `DeviceSlot` のヘッダが `justify-between` 前提で、右側ボタン群に `shrink-0` 相当の逃がしがなく、左側タイトルも十分に折り返せていない
3. タイトル行と右上操作群を 1 段で保とうとしているため、表示倍率増加時に操作群が画面外へ押し出される

## 改修方針案

### 方針 A

- スロットヘッダを狭幅時のみ 2 段化する
- 1 段目にタイトル、2 段目に右上操作群を置く
- アクセシビリティと確実性は高いが、見た目の変化はやや大きい

### 方針 B

- 1 段構成は維持しつつ、タイトルを先に縮めて右上操作群の可視性を守る
- 必要に応じてタイトルの折り返しや編集ボタン位置を調整する
- 見た目の連続性は高いが、極端に狭い幅では限界がある

### 方針 C

- 方針 B を基本とし、閾値以下でのみ方針 A に切り替える
- デスクトップでは現在の見た目を維持しやすく、ズーム時の保険もかけやすい
- 第一候補

## 実装時の具体ポイント

- スロットヘッダに `flex-wrap` またはブレークポイント付きの縦積みを導入する
- タイトル領域に `min-w-0` だけでなく、見出し本体の折り返し許可や必要な `flex` 制御を与える
- 右上操作群は常に表示域内へ残ることを優先し、必要なら `shrink-0` と行移動を組み合わせる
- アプリ全体の `min-w-full w-fit` は、表示域外への張り出し原因なら `w-full` 基調へ見直す
- キーボード本体の横幅が大きくても、少なくともスロットヘッダ操作は先に壊れないよう、ヘッダと本体の責務を切り分ける

## 検証観点

- 通常幅で既存の見た目が大きく崩れない
- ブラウザ幅を狭めても、各スロット右上の `Display Settings` ボタンに到達できる
- 表示倍率を上げても、ヘッダ操作群がカード外へ逃げにくい
- `STACK` / `GRID` の両方で破綻しない
- タイトル編集中でも入力欄と右上操作群の競合が悪化しない
- 設定パネル展開時も上部の操作導線が維持される

## 実装しないこと

- ハンバーガーメニューの情報設計そのものの再設計は行わない
- キーボード描画ロジックの大規模改修までは広げない

## 次の着手順

1. `js/app.js` の最上位幅制約を最小変更で見直す
2. `js/components/DeviceSlot.js` のヘッダを狭幅耐性のある構造へ調整する
3. 必要なら設定パネルとアクション列の折り返し規則を追加する
4. Playwright もしくはブラウザ実機で、狭幅と拡大表示の両ケースを確認する

## 現在の状態

- 専用ブランチ作成済み
- `js/app.js` の最上位幅制御を `w-full` 基調へ寄せ、狭幅時に内容幅へ引っ張られにくいよう着手した
- ヘッダ追従維持のため、最上位コンテナの横オーバーフロー制御は `overflow-x: hidden` 系ではなく `overflowX: clip` へ寄せる
- `js/components/DeviceSlot.js` のスロットヘッダを、通常時は横並び、狭幅時は縦積みへ逃がせる構造へ着手した
- 表示オプション内の `INPUT e*` 行について、ラベル・入力種別・バリアントをグループ単位で折り返せる構造へ調整した
- `node --check js/app.js` と `node --check js/components/DeviceSlot.js` は通過した
- Playwright で幅 `520px` と `420px` を確認し、`Sample Numpad` を含むスロットで右上の `Display Settings` ボタンが表示領域内に残ることを確認した
