# 実装計画

## 目的

`Text` 表示かつ `Mac` スタイル時に、オフセット副入力の修飾ラベルが記号化されず、明示的なテキスト表記で表示されるようにする。

## 今回の対象

- `js/utils/labelParser.js`
- 必要に応じて修飾ラベル共通定義

## 現状把握

- 前回の修正で Mac 用 SVG 分岐は `Fluent` 限定にできている。
- ただし `labelParser` 側で、Mac 修飾ラベルを `displayMode` に関係なく記号優先で生成している箇所がある。
- そのため `Text` モードでも `⌘` や `⌥` のような記号が副入力へ渡り、見た目上は依然としてアイコン表示に見える。

## 方針

- Mac 修飾ラベルの記号化は `Fluent` モード時だけ許可する。
- `Text` モードでは `CMD` / `OPT` などのテキスト表記へ揃える。
- 単一修飾と複合修飾の両方で同じ条件を適用する。

## 実施手順

1. `labelParser` の Mac 修飾ラベル生成条件を見直す。
2. `Text` モードでは記号化しないよう修正する。
3. 軽量な構文チェックで破綻がないことを確認する。

## 実施済み

- [x] SVG 分岐以外に、記号化が残る経路を特定した。
- [x] `Text` モードで記号化しないよう修正した。
- [x] 軽量な構文チェックで破綻がないことを確認した。

## 完了条件

- `Text` 表示かつ `Mac` スタイル時に、オフセット副入力が `CMD` / `OPT` などのテキストで表示される。
- `Fluent` 表示時の Mac 記号・SVG ルールは維持される。
