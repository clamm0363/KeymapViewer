# 実装計画

## 【対応中】CALLOUTS がデバイス描画に重なる不具合修正（2026-06-04）

### 問題
- `CALLOUTS` を `ON` にした常時表示カードが、左右の空き領域ではなくキーボード筐体やキー描画の上に重なって表示されることがあります。
- マウスホバーの `tooltip` は別系統で、今回の不具合対象は常時表示の `CALLOUTS` のみです。

### 原因
- 既存の `calloutLayout` は「スロット全体の幅に収まるか」だけで左右配置可否を判定していました。
- そのため、キーボード全体の外側ではなく、デバイス本体の占有帯の上でも「左右に置ける」と誤判定されていました。
- 左右に置けない場合の `below` fallback はありますが、「本体に重なるがコンテナには収まる」ケースは fallback 条件に入っていませんでした。

### 修正方針
- 各キーの横ではなく、キーボード全体の外側にある左帯・右帯へ置けるかどうかで side 配置を判定します。
- 左帯または右帯に十分な幅がない場合だけ `below` fallback へ切り替えます。
- これにより、inner key の近くにある空きっぽい場所へ無理に置かず、常にデバイス外側へ逃がします。
- `below` fallback 時の `bottomPadding` は固定下限を使わず、実際に callout がはみ出した高さぶんだけ確保するようにします。

### 実装ステップ
1. `js/utils/calloutLayout.js` の side 配置判定を、キー単位基準から「キーボード全体の外側帯」基準へ変更する
2. `below` fallback 条件を新しい side 判定に合わせて維持する
3. `test/utils/calloutLayout.test.js` に、inner key が本体外側へ逃げることと、外側帯が足りない場合に `below` へ落ちることを追加する
4. `npm run test` と `npm run lint` を実行して回帰確認する
5. `below` 時に SLOT 下部へ不要な大余白が出ないことを確認する

### 確認ポイント
- split 配置でも中央や筐体上へ重ならず、外側帯か下方にのみ出ること
- `below` のとき `bottomPadding` が不足して切れたり重なったりしないこと
- `below` のとき必要最小限の余白だけが追加され、SLOT 下部が不自然に広がらないこと
- side 配置が厳しすぎて、本来左右へ出せるケースまで過剰に `below` へ落ちないこと

### 実施メモ
- `calloutLayout` の side 配置は、各注釈キーの位置ではなく、注釈付きキー全体から算出した左右外側帯を基準に再計算する方針です。
- これにより、常時表示 CALLOUTS を「キーボード本体の外に置く」挙動へ寄せます。

## 【計画】CALLOUTS の視認性とコネクタ線デザイン改善（2026-06-04）

### 目的
- 現在の CALLOUTS はカード自体のトーンは既存 UI に馴染んでいる一方、コネクタ線の向きがばらつくことで全体がやや散漫に見えます。
- デザインの方向性は維持したまま、情報の階層と接続感を少しだけ強め、一覧表示時の読みやすさを上げます。

### 改善方針
- カード本体は大きく作り直さず、余白・文字階層・境界の強さを微調整します。
- コネクタ線は自由な最短線ではなく、`短い垂直線 + カードへ向かう斜め線` の2段構成へ寄せます。
- すべての callout で「まず下へ降りる」という共通ルールを持たせ、見た目の秩序を作ります。
- 追加調整として、コネクタ線の角度が大きい場合は card 上辺ではなく側面へ接続し、必要なら折れ曲がりを最大 2 回まで許容します。
- 始点ドットはより小さく控えめにし、キー枠線中央へ正確に重ねます。

### デザイン方針
- カード:
  - 上下 padding を少しだけ詰め、複数並んだときの重さを軽減する
  - タイトルを本文より一段強くし、一覧でも要点が拾いやすい階層にする
  - 本文のコントラストをわずかに抑え、カード全体の密度バランスを整える
- コネクタ線:
  - キーの注釈ドット付近から、まず一定長の垂直線を下へ伸ばす
  - その終点からカード上辺中央またはカード寄りの接続点へ斜め線を引く
  - 線色は現状より少しだけ明るくし、始点アンカーを認識しやすくする
  - 必要なら始点に小さな発光ドットを追加し、「どのキーの説明か」を即座に追えるようにする

### 実装方針
- `js/utils/calloutLayout.js`
  - 各 callout に対して、従来の `lineStart` / `lineEnd` だけでなく、0〜2 個の折れ点を持てる connector path を算出する
  - `below` callout 用に、始点から一定距離だけ真下へ降ろしたうえで、上辺接続か側面接続かを選ぶ
  - 始点座標はキー外形の推定値ではなく、実際の keycap frame の `3px` 枠線中央へ合うよう補正する
- `js/components/KeyCalloutOverlay.js`
  - SVG の線描画を `connector path` ベースへ変更し、最大 2 回までの折れを描画できるようにする
  - 始点ドットは小型化し、必要なら glow をごく弱くする
  - カード内の typography / padding / border / shadow を微調整する
- `test/utils/calloutLayout.test.js`
  - `below` callout に connector path が追加されること
  - 角度が大きいケースでは側面接続へ切り替わること
  - connector path の折れ点数が最大 2 回に収まること
  - 既存の side / below 判定が壊れていないこと

### 実装ステップ
1. `below` callout のコネクタ線仕様を `垂直 + 斜め` の2段構成で固定する
2. `calloutLayout` に中継点座標を追加する
3. `KeyCalloutOverlay` の SVG 描画を 2セグメント構成へ変更する
4. カードの余白・タイトル・本文コントラストを小さく調整する
5. 必要なら始点ドットを追加する
6. テスト更新後に `npm run test` と `npm run lint` を実行する

### 確認ポイント
- コネクタ線の始点方向が揃い、複数 card が並んでも視線誘導が自然に見えること
- 折れ点や斜め線が密集して、逆にごちゃつかないこと
- split レイアウトでも card と線の対応関係が直感的に追えること
- 既存の dark テーマの上品さを崩さず、説明 UI として少しだけ整理感が増すこと
- side 接続時の始点が、キー左右の枠線中央に正しく乗って見えること
- CALLOUT がサイトヘッダより前面に出ず、ヘッダ領域では正しく隠れること
