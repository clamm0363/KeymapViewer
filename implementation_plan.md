# 実装計画

## 目的

`MAPPING` ボタン押下時に、従来どおり `.json` ファイルを読み込む経路に加えて、接続済みデバイス本体からマッピング情報を読み出して画面へ反映する経路を追加する。

今回のゴールは次の 4 点。

- サービス初回表示時には接続ダイアログを出さない
- `MAPPING` 押下時に「JSON 読み込み」か「接続デバイスから取得」かを選べる
- 接続デバイス読込は、選択中スロットの描画だけを置き換える形で反映する
- 既存の `devices` 配列と `DeviceSlot` UI を活かしつつ、マッピング取得元の選択肢を増やす

## 現状認識

### UI / 状態管理

- `js/components/DeviceSlot.js`
  - `MAPPING` は hidden file input を持つ `label` で、現状は JSON ファイル読み込み専用
  - モーダルは `MacroModal` / `HelpModal` / `LinksModal` / `ExportModal` があり、追加モーダルを置く余地はある
- `js/app.js`
  - デバイス表示は `devices` 配列で一元管理されている
  - `handleFile()` / `processDroppedFile()` が JSON 由来の `design` / `keymapJson` 読み込みを担当している
  - 起動時は保存状態がなければ `SampleLayouts/` を読み込んでサンプル表示を生成する
  - スロットは最大 4 件までという前提がある

### デバイス識別に使えそうな既存データ

- `json/` 配下の定義 JSON には `vendorId` / `productId` を持つものがある
- `json_mapped/` 配下の一部レイアウトには `vendorProductId` があり、接続デバイスとの対応付けに再利用できる可能性がある
- ただし現時点では、これらを検索・解決する共通ロジックは未実装

### WebHID 残骸の有無

- `navigator.hid` / `requestDevice()` / `HIDDevice` などの直接利用は、今回確認した `js/` / `scripts/` 配下には見当たらなかった
- 「以前の実装の残骸」があるとしても、少なくとも現行の起動導線には組み込まれていない

## 実装方針

### 1. `MAPPING` 導線を「ファイル読込」と「接続デバイス読込」に分岐させる

- `DeviceSlot.js` の `MAPPING` ボタンを、即 file input を開く UI から変更する
- 押下時に小さな選択モーダルを開き、以下の 2 経路を選べるようにする
  - `JSONファイルを読み込む`
  - `接続デバイスから取得する`
- file input 自体は残し、選択モーダルからのみ起動する

### 2. WebHID 依存の処理は専用モジュールへ隔離する

- `js/utils/hid/` を今回の実装開始地点として使う
- 役割を UI から切り分ける
  - 対応デバイス列挙
  - ユーザー操作起点での `requestDevice()`
  - 接続済みデバイスの再取得
  - デバイス識別子から既存レイアウト定義を引く処理
  - デバイス本体から読み出したマッピングを `keymapJson` 互換データへ正規化する処理
- `App` や `DeviceSlot` には、HID レポート処理の詳細を持ち込まない

### 3. 起動時には問い合わせず、`MAPPING` 押下時だけ権限要求する

- 初回表示や `App` マウント時に `navigator.hid.requestDevice()` は呼ばない
- 必要に応じて `navigator.hid.getDevices()` は「既に権限付与済みデバイスの静かな確認」に限定して使う
- 明示的な選択操作後にのみ `requestDevice()` を使い、ブラウザの接続ダイアログを出す

### 4. 接続デバイス読込は、選択中スロットだけを更新する

- 「接続デバイスから取得」を選んだ際は、その `MAPPING` ボタンを押したスロットだけを更新対象にする
- 想定フロー
  - 対象スロット ID を保持したまま、既権限デバイスを取得する
  - 必要なら `requestDevice()` で追加許可する
  - 対応可能なデバイスを選択または確定する
  - 読み出した結果を `design` + `keymapJson` を持つ表示モデルへ変換する
  - 対象スロット 1 件に対して `updateDevice()` 相当の経路で反映する
- 複数接続デバイスの同時表示や一括取込は、今回は保留にする
- まずは「どのスロットを置き換える操作なのか」が常に明確な UI を優先する

### 5. 対応デバイス判定は VID/PID ベースで始める

- 既存 JSON から `vendorId` / `productId` / `vendorProductId` を読めるインデックスを生成する
- WebHID の `vendorId` / `productId` と照合し、対応するレイアウト定義を引く
- 対応表が未整備の機種は、最初は「未対応」として明示し、誤読込を避ける

### 6. デバイス読込結果は既存の JSON 読込結果と同じ形へ正規化する

- 既存描画系は `design` と `keymapJson` を前提に動いている
- そのため WebHID 取得結果も最終的にはこの形に寄せる
- 具体的には以下を揃える
  - `design`: レイアウト、マトリクス、エンコーダ情報など
  - `keymapJson`: `layers`、`macros`、必要なら `encoders`
- こうしておくと `Keyboard` / `MacroModal` / 既存 tooltip 系を大きく崩さずに済む

## 想定する変更対象

- `js/app.js`
  - `MAPPING` ソース選択モーダルの state 管理
  - ファイル読込とデバイス読込の統合入口
  - 対象スロット単位のデバイス反映ロジックの追加
- `js/components/DeviceSlot.js`
  - `MAPPING` ボタンの導線変更
  - 「このスロットを置き換える操作」であることが分かる UI 文言調整
- `js/components/Modals/`
  - 新規モーダル追加
  - 例: `MappingSourceModal.js`
- `js/utils/hid/`
  - WebHID の列挙、接続、変換、エラー整形を担う新規モジュール群
- `js/utils/` または新規小モジュール
  - VID/PID と既存 JSON 定義を結び付けるインデックス生成 helper

## 実装ステップ案

1. 既存 `MAPPING` ボタンを file input 直結から外し、ソース選択モーダルを追加する
2. 既存の JSON 読込処理をモーダル経由でも使えるように整理する
3. `js/utils/hid/` に WebHID 入口を作り、`requestDevice()` をユーザー操作限定で呼ぶ構造を入れる
4. 既存 JSON から VID/PID 対応表を組み立てる helper を追加する
5. 接続デバイスごとに `design` / `keymapJson` へ変換する処理を追加する
6. 選択したデバイス読込結果を対象スロットへ反映するロジックを `App` に追加する
7. エラー時のメッセージを整理する
8. 静的構文チェックと、可能な範囲の手動確認を行う

## 検証観点

- サービスを開いただけでは接続ダイアログが出ない
- `MAPPING` 押下時のみ接続手段の選択 UI が出る
- `JSONファイルを読み込む` を選んだ場合、既存の読込動作が壊れていない
- `接続デバイスから取得する` を選んだ場合、権限付与済みデバイスだけで読めるケースと、追加許可が必要なケースの両方で破綻しない
- 対応デバイスが複数見えていても、選択中スロットだけが更新される
- 未対応デバイスや読込失敗時に、無言で何も起きない状態にならない
- 最大 4 スロット制約と衝突する場合の扱いが明確である

## 主要リスク

### 1. デバイス本体からのマッピング取得プロトコルが未確定

- 既存コード上には、実機からどの HID レポートを使ってレイヤー情報を読むかの実装がない
- VID/PID で機種特定できても、実際のマッピング取得手順が機種依存なら追加調査が必要
- このため、実装は
  - `デバイス検出と機種対応付け`
  - `実マッピング読出し`
  の 2 層に分けて進める

### 2. 接続先デバイスの選択 UI が必要になる可能性がある

- 複数の権限付与済みデバイスや複数の新規選択デバイスが見える場合、どれを対象スロットへ反映するかを曖昧にできない
- 実装時は
  - 最初に 1 台だけ選ばせる
  - もしくは対象候補一覧をモーダル内に出す
  のどちらかへ寄せる

### 3. サンプル自動読込との整合

- 現状は初回表示でサンプルが埋まる
- 実機読込を主目的にする場合、サンプル表示と実機表示の関係を整理しないと混乱しやすい
- 今回は対象スロットのみを置き換える前提なので、サンプルが残るスロットと実機スロットが混在するケースを確認する

## 現時点の判断

- まずは UI 導線と状態管理を整え、`MAPPING` からのみ WebHID を起動する原則を固める
- WebHID 処理は `js/utils/hid/` に隔離し、既存コンポーネントへ直接書き込まない
- 実マッピング取得プロトコルは最大の不確定要素なので、実装ではここを段階的に切り分ける
- 一括取得は保留とし、今回は「選択中スロットだけを置き換える」方針で進める

## 追加対応メモ

- WebHID 読み出し後の `0x78xx` 系キーコードは、`QK_LIGHTING` 配下の複数サブレンジ
  - `QK_BACKLIGHT`
  - `QK_LED_MATRIX`
  - `QK_UNDERGLOW`
  - `RGB_MODE_*`
  - `QK_RGB_MATRIX`
  に分かれているため、単一の RGB テーブルで雑に解釈しない
- Keychron Q0 Plus で確認された `RGB M P / RGB M B / RGB M R` の不一致は、この `QK_LIGHTING` 範囲の誤読が主因
- 既存 JSON 読込と同じ見え方を優先し、WebHID 側でも可能な限り QMK/VIA の別名表記へ寄せる
- ZUOYA GMK26 については `json/` 配下の定義 JSON を WebHID 用登録表へ追加し、`vendorId=0x36B0` / `productId=0x3083` で読込対象に含める
- 定義ファイル名に特殊空白が含まれていると保守しづらいため、登録時に ASCII ベースの素直なファイル名へ寄せる

## 次フェーズ設計案

### 目的

- 個人所有デバイス用の定義と、公開収集した定義を分離する
- WebHID 対応デバイスの登録表を手書き管理から減らす
- 未対応デバイスでも、ユーザが定義 JSON を指定して読込を試せるようにする
- 外部サイトへの都度依存は避けつつ、将来的な公開データ拡充に備える

### 基本方針

- 現在の `json/` と `json_mapped/` は、個人所有デバイス由来のファイル群として扱う
- 公開収集データは別フォルダへ分離する
- アプリ起動時または初回読込時に、複数フォルダを横断して `vendorId / productId -> layout definition` の索引を自動生成する
- 接続デバイスが索引に無い場合は、「未対応」で終わらせず、定義 JSON をユーザが指定して試せるフォールバックを出す
- ネット上をその場で探索する実装は、常設の主導線にはしない

### 推奨ディレクトリ構成

- `json/`
  - 個人所有デバイスの VIA 定義 JSON
- `json_mapped/`
  - 個人所有デバイスから保存したマッピング済みデータ
- `json_public/`
  - 公開ソースから収集した VIA 定義 JSON
- `json_public_verified/`
  - 実機または十分な確認を経て採用した公開定義
- `json_public_unverified/`
  - まだ未検証だが候補として保持する公開定義

### 最小構成案

- 実装初期はフォルダを増やしすぎず、まずは次の 2 系統で始める
  - `json/`
  - `json_public/`
- `verified / unverified` の区分は、必要になってから増やす

### 登録表生成の方針

- `js/utils/hid/deviceRegistry.js` の手書き配列を将来的に縮小する
- 代わりに、定義 JSON 群から次の情報を抽出して registry を構築する
  - `vendorId`
  - `productId`
  - `name`
  - `layoutPath`
- 走査対象フォルダは配列で持ち、優先順位を付ける
  - 例: `json/` を優先し、同一 VID/PID があれば `json_public/` より先に採用する
- 重複時は次のルールを持つ
  - 個人用定義を優先
  - 同じフォルダ内で重複したら警告を出す
  - 可能なら読み込み時に `console.warn` で重複元を可視化する

### 未対応デバイス時のフォールバック

- `MAPPING -> 接続デバイスから取得する` 実行時に、VID/PID が registry に無ければ次の選択肢を出す
  - `このまま閉じる`
  - `定義JSONを選択して試す`
- `定義JSONを選択して試す` を選ぶと、VIA 定義 JSON 用の file picker を開く
- その JSON が最低条件を満たす場合のみ、一時的にそのスロットへ適用して WebHID 読込を続行する
- 最低条件
  - `vendorId`
  - `productId`
  - `matrix`
  - `layouts`

### 一時定義の扱い

- ユーザがその場で指定した定義 JSON は、まずは永続保存しない
- 読込成功後に、必要なら将来拡張として
  - `この定義をローカル登録する`
  - `次回以降も使う`
  を追加できる余地を残す

### 公開データ収集の扱い

- 主導線として「接続時にネットを検索する」は採らない
- 理由
  - CORS や配布元制約でブラウザから安定取得できない
  - 品質の低い JSON や古い JSON が混ざる
  - 外部サイトの可用性にサービスが引きずられる
- 代わりに
  - 収集は事前バッチまたは手動メンテで行う
  - 実行時はローカルカタログだけを見る

### 将来の拡張余地

- 収集スクリプトを用意し、公開 JSON を `json_public/` へ正規化して配置する
- registry をビルド時生成ファイルにする
- 公開定義ごとにメタ情報を持たせる
  - `source`
  - `verified`
  - `lastCheckedAt`
  - `notes`
- UI 上で「この定義は未検証」を表示できるようにする

### 想定変更対象

- `js/utils/hid/deviceRegistry.js`
  - 手書き registry から、複数フォルダの索引化ロジックへ整理
- `js/app.js`
  - 未対応デバイス時のフォールバック導線追加
- `js/components/Modals/`
  - 未対応デバイス用モーダル、または既存 `MappingSourceModal` の拡張
- `json_public/`
  - 公開定義の保存先
- 必要なら `js/utils/`
  - 定義 JSON の妥当性チェック helper

### 実装順案

1. `json_public/` を追加し、フォルダ責務を明確化する
2. `deviceRegistry` を複数フォルダ走査型へ置き換える
3. 個人用定義優先の重複解決ルールを入れる
4. 未対応デバイス時に `定義JSONを選択して試す` 導線を追加する
5. VIA 定義 JSON の最低限バリデーションを追加する
6. 実機未登録デバイスでフォールバック読込を確認する

### 次エージェント向けメモ

- 今回は設計のみで、コード変更はここまでに留める
- 実装時は既存の `MAPPING` モーダル導線を壊さず、段階的に追加する
- `json_mapped/` は保存済みマッピングであり、VIA 定義 JSON の代替としては扱わない前提を維持する
