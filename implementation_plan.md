# 実装計画

## 目的

デバイス表示の拡大率がブラウザ倍率や表示領域に対して非直感的に変化している現状を見直し、次の 2 点を両立する。

- 可能な範囲では、どのデバイスも `1u` キーが同じ見た目サイズで表示される
- 各スロットごとに、ユーザーが個別に表示倍率を調整できる

ただし、小画面や大型レイアウトでは完全統一を優先しすぎると視認性が落ちるため、一定閾値を超える場合は大型デバイス側を自動縮小して収める方針を採る。

## 作業ブランチ

- `fix/responsive-slot-header-access`

## 現状認識

- `js/components/Keyboard.js` では、現在 `ResizeObserver` により「コンテナ幅に収まるスケール」を計算している
- そのため、同じ `1u` 幅を持つデバイスでも、横幅や外枠幅の違いで見た目サイズが変わる
- `forcedScale` は export 用の固定値として存在するが、通常表示ではユーザー倍率レイヤーを持っていない
- スロット単位の表示設定には `displayMode` `theme` `keyStyle` などはあるが、キーボード表示倍率の永続設定はまだない

## ゴール仕様

### 基本表示

- 通常時は、全スロットで `1u` キーの表示サイズが可能な限り一致する
- この基準サイズは「全デバイス共通のベース `1u` 表示サイズ」として扱う

### 自動縮小

- ただし、大型デバイスがスロット横幅に収まらない場合は、基準サイズ維持を打ち切って自動縮小する
- テンキーや小型デバイスを大型キーボードに無理に合わせて小さくしすぎない
- 優先順位は次のとおりとする
  1. `1u` 基準サイズで表示する
  2. 収まらない場合だけ大型デバイス側を自動縮小する
  3. その上でユーザー倍率を乗算する

### ユーザー倍率

- 各スロットに表示倍率スライダーを配置する
- スライダーは絶対倍率ではなく「基準表示に対する倍率」とする
- 内部的な最終倍率は `autoFitScale * userScale` とする
- スロットごとの倍率は保存され、再読込後も復元される

## 想定 UI

- 配置場所
  - 各スロットの `Display Settings` 内、表示設定群の近く
- 表示内容
  - `ZOOM` または `SCALE`
  - スライダー
  - 現在値のパーセント表示
  - `100%` リセット操作
- 初期値
  - `100%`

## 実装方針

### 1. スケール責務の分離

- `Keyboard.js` の現在の `scale` を、責務ごとに分けて整理する
- 具体的には次の 2 つに分離する
  - `autoFitScale`
    - スロット幅に収めるための自動縮小値
  - `userScale`
    - スロット設定から来るユーザー指定倍率
- 最終描画倍率は `finalScale = baseScale * autoFitScale * userScale` ではなく、今回の設計では `baseScale` を事実上 `1u` 基準に吸収し、`finalScale = autoFitScale * userScale` として扱う想定

### 2. `1u` 基準サイズの導入

- `UNIT = 56` は内部座標系としてすでに存在するため、まずはこれを「全デバイス共通の見た目基準」に寄せる
- 現状のようにコンテナ幅から直接倍率を決めるのではなく、まず `1u` が同じ見た目になる基準倍率を固定し、その後に必要なときだけ `autoFitScale` を下げる
- つまり「常に fit から逆算する」のではなく、「基準表示から必要時のみ縮める」へ反転する

### 3. 自動縮小の閾値設計

- 大型レイアウトが極端に縮みすぎないよう、次の設計を行う
  - スロット内で利用可能な横幅を計算する
  - 基準表示時のキーボード幅が利用可能幅を超えた場合のみ `autoFitScale < 1` にする
  - 超えない場合は `autoFitScale = 1`
- これにより、小型デバイスを大型デバイス基準で不必要に縮小しない

### 4. スロット設定への保存

- `App` / `DeviceSlot` 側の device state に、たとえば `displayScale` のような項目を追加する
- `createEmptyDevice()`、サンプル読込、URL 共有復元、ローカル保存復元のすべてでこの項目を扱う
- 共有 URL に含めるかは実装時に判断するが、少なくともローカル保存対象には含める

### 5. UI 操作系の追加

- `DeviceSlot.js` の `Display Settings` に倍率スライダーを追加する
- 追加候補仕様
  - 範囲: `60% - 160%` もしくは `50% - 150%`
  - 刻み: `5%`
  - 初期値: `100%`
  - リセットボタン: `100%`
- 表示ラベルは既存 UI の密度に合わせ、短く保つ

## 変更対象

- `js/components/Keyboard.js`
  - スケール計算の再設計
  - `autoFitScale` と `userScale` の合成
- `js/components/DeviceSlot.js`
  - 表示倍率スライダー UI の追加
- `js/app.js`
  - device state の新規項目
  - 初期サンプル読込時の既定値付与
  - 永続化対象の更新
- 必要に応じて
  - `js/utils/helpers.js`
  - 共有 URL 復元処理
  - export 時の `forcedScale` との責務整理

## 検討ポイント

### 1. スライダー範囲

- あまり広くすると崩れやすい
- 狭すぎると調整の自由度が足りない
- 第一候補は `60% - 160%`

### 2. export との関係

- export ではすでに `forcedScale: 1.0` を使っている
- 今回の `userScale` を export に反映するかどうかを明確に決める必要がある
- 第一候補は「画面上の見た目に合わせるため、export にも反映する」

### 3. 極端に縦長・横長なレイアウト

- 横幅基準だけでなく、必要なら高さ方向も確認する
- ただし第一段階では横幅起点の自動縮小を優先する

## 検証観点

- テンキーと 100% キーボードで、通常時の `1u` 見た目サイズが概ね一致する
- 大型キーボードだけが小画面で自動縮小され、小型デバイスが不必要に縮小されない
- スロットごとのスライダー操作が他スロットへ波及しない
- スライダー値が再読込後も保持される
- `STACK` / `GRID` の両方で破綻しない
- 表示倍率を上げても、前回対応したヘッダや設定 UI のレスポンシブ性が壊れない
- export の見た目が意図どおりか確認できる

## 実装しないこと

- 今回はまだコード変更を行わない
- デバイスごとの物理寸法や実機サイズ比率の厳密再現は行わない
- キーキャップ個別の文字サイズロジックまでは再設計しない

## 実装順

1. `Keyboard.js` の既存スケール計算を読み替え、`autoFitScale` と `userScale` の責務を分ける
2. device state に表示倍率プロパティを追加する
3. `DeviceSlot.js` に倍率スライダー UI を追加する
4. `1u` 基準表示と大型デバイス自動縮小の合成を実装する
5. 永続化と必要なら共有 URL / export 連携を整える
6. 小型デバイスと大型デバイスの比較表示を確認する

## 現在の状態

- `Keyboard.js` に `userScale` レイヤーと `autoFitScale` レイヤーを分離する着手を行った
- 自動スケールは「収まらない大型デバイスだけ縮小する」方向へ変更し、小型デバイスの自動拡大は止める着手を行った
- `App` / `DeviceSlot` にスロット別 `displayScale` state とスライダー UI を追加する着手を行った
- スライダーや各種設定操作がスロット並べ替えドラッグに奪われないよう、スロット全体ドラッグをやめてヘッダ内の専用ドラッグハンドルからのみ並べ替えを開始する構成へ切り替える
- `node --check js/app.js` `js/components/DeviceSlot.js` `js/components/Keyboard.js` は通過した
- Playwright で `SCALE` スライダーが表示されることを確認した
- Playwright でスライダー操作後、対象スロットの `keyboard-inner` transform だけが変化し、他スロットへ波及しないことを確認した
- Playwright でリロード後のページを再確認し、スライダー値がドラッグ操作で変化することを再確認した
- その後、ユーザー環境での再発報告を受け、イベント抑止頼みではなく専用ドラッグハンドル方式へ切り替えて干渉源を構造的に分離した
- ドラッグハンドルはタイトル左では見つけにくいため、スロット上部枠線の中央に広めの可視ハンドルとして再配置し、フレーム意匠の一部として認識しやすくする
- スロット入れ替え時の視覚フィードバックはハンドル単体ではなくスロット全体が動いて見えるようにし、ドラッグ画像と半透明化対象をカード全体へ切り替える
- 下部の `ADD SLOT` タイルは廃止し、同じ位置に「表示中の 1u キーサイズをそろえる」ワンボタン操作を追加する
- そろえ処理は `Keyboard` から各スロットの `autoFitScale` を親へ返し、その最小値へ全スロットの表示倍率を合わせる
- そろえ処理を確実に成立させるため、手動スライダー下限も大型デバイス基準まで下げられる値へ見直す
- その後、設置位置の解釈違いを修正し、そろえボタンは下部タイルではなくヘッダの旧 `ADD SLOT` 位置へ移設する
- さらに、空きスロット位置の役割は維持し、下部には従前どおり `ADD SLOT` タイルと追加機能を戻す
- `SCALE` 右側に「他スロットのキーサイズ変更へ追従する」チェックを追加し、有効時はそのスロットのスライダーとリセット操作をグレーアウトして直接変更できないようにする
- 追従処理は親 state で `displayScale` 更新を受けたタイミングに行い、追従対象スロットの最終キーサイズが変更元と一致するよう補正する
- ヘッダのボタン列は、狭幅時に一部ボタンだけ改行で縦長になった場合でも、同じ行の他ボタン群が同じ高さへそろうようにする
- グローバルテーマ切替トグルも同じ高さルールへ合わせ、外枠とアイコンボタンが縦方向ストレッチに追従するようにする
- 今回の追加改善として、`LAYOUT` `MAPPING` `MACROS` `SHARE` `EXPORT` の操作列を独立行のまま残さず、`SLOT` / デバイス名ヘッダ領域へ移設して縦方向の占有を減らす
- `LAYER` 選択も同じヘッダ配下の操作帯へ統合し、キーボード表示本体がより上に詰めて表示されるようにする
- その際、各操作ボタンは横幅・余白・文字サイズを詰め、1 行または自然な折り返しでヘッダ幅に収まりやすい密度へ再調整する
- 配置解釈の差分に対応し、統合後の操作帯は `LAYER` を左寄せ、そのほかの `LAYOUT` `MAPPING` `MACROS` `SHARE` `EXPORT` は右寄せに分離して視線導線を整理する
- さらに右寄せ基準はキーボード表示領域の端ではなく、スロット右上のメニューアイコン列に合わせ、操作帯全体の右端がヘッダ操作列とそろって見えるようにする
- 右端そろえが効かない場合は、余白や `justify` より先に DOM 階層を確認し、対象の操作帯をヘッダグリッド直下へ出して `col-span` が実際に効く構造へ直す
- ヘッダ上部の `GRID` / `STACK` トグルはテキストではなく既存の Fluent SVG アイコンへ置き換え、2 つのアイコントグルを 1 区画にまとめる
- あわせて、従来トグル右側にあったセパレータは左側へ移し、`MATCH KEY SIZE` とレイアウトトグル群の境界として見せる
- `LIGHT` / `DARK` トグルも WebFont 文字のまま残さず、既存の Fluent SVG アイコンへ差し替えてヘッダ内のアイコン表現を統一する
- `SLOT` 領域の「えんぴつ」「ごみばこ」「ハンバーガー」も、独自インライン SVG のまま残さず、Fluent SVG ライブラリ経由の描画へ統一する
- `DARK` トグルの夜テーマ表現は「日の入り」系ではなく、ローカルの Fluent UI System Icons クローンを参照した月アイコンへ差し替える
- `SLOT` 領域の 3 アイコンは見た目密度に合わせて少し拡大し、メニューは三本が同じ長さの典型的なハンバーガー SVG へ差し替える
- `SLOT 1`、デバイス名、右上アイコン群のヘッダ行は上揃えではなく中央線基準で整列し、ラベルとアイコンの視覚的なセンタリングをそろえる
- センタリング違和感の検証用として、`SLOT 1` ヘッダ 1 行目の中心に左右いっぱいの基準線を一時表示し、ラベル群と右端アイコン群が同じセンターライン上にあるかを可視化する
- 基準線の位置ずれから、センタリング基準を `slot-header` 全体ではなく「1 行目そのもの」の行ボックスへ合わせ直し、左群と右群を同一行コンテナ内で `items-center` 整列する構造へ改める
- グリッド表示では `LAYER` と操作ボタン群を 1 行で奪い合わず、上段に左寄せの `LAYOUT` `MAPPING` `MACROS` `SHARE` `EXPORT`、下段に左寄せの `LAYER` を置く 2 段構成へ切り替える
- その条件分岐は `layoutMode` を `DeviceSlot` へ渡して行い、スタック表示時の 1 行構成は維持したまま、グリッド表示時だけ操作帯の順序・整列方向・ボタン密度を最適化する
- `LAYER` 数が多いデバイスでは全ボタンを一度に並べず、一定数を超えたら前後ページボタン付きのページング表示へ切り替えて操作帯の横破綻を防ぐ
- 検証用として `SampleLayouts/sample_numpad.json` を `LAYER 12` まで拡張し、多レイヤー時のページ送り・戻りとキーボード表示の追従を確認できる状態にする
- `GRID` / `STACK` 切替時の表示倍率は 1 つを共用せず、各スロットがレイアウトモードごとの `displayScale` を別々に保持して、切替後の横幅に対して自然なサイズへ戻れるようにする
- スライダー、キーサイズ追従、共有データ、永続化も「現在のレイアウトモードの倍率」を更新対象にそろえ、モード切替で片方の表示調整がもう片方を壊さない状態にする
- ただし切替先モードの見た目は前回値をそのまま再利用せず、`GRID` / `STACK` 切替時には対象モードの倍率を `100%` ボタン相当へ自動リセットし、そのモードの `autoFitScale` を素直に効かせる
