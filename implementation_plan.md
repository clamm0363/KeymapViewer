# 実装計画

## 【計画】変数名の意味明確化メンテナンス（2026-06-05）

### 目的
- 実際の役割に対して意味が曖昧な変数名、誤読しやすい短縮名、責務が伝わりにくい一時変数名を見直し、コードの読解コストを下げる
- 挙動変更を伴わず、保守性とレビューしやすさを高める

### 前提
- 今回の主対象は、関数内ローカル変数、分岐用フラグ、計算途中の一時値、props 名、内部 helper の引数名
- 外部仕様に直結する名前は安易に変更しない
  - 例: JSON のキー名、保存データのプロパティ名、他ファイルから参照される export 名、UI から前提化されているデータ構造
- 既存の略称がドメイン上で十分に定着している場合は、無理に長文化しない
  - 例: `RGB`, `JIS`, `SVG`, `idx` のように文脈上明確なもの

### 命名見直しの判断基準
- その変数名だけで「何が入るか」「何のために使うか」が概ね想像できるか
- 似た責務の別変数と混同しないか
- 真偽値なら `is` / `has` / `should` など、意味に合う接頭辞になっているか
- 配列・Map・表示モデルなど、値の種類が名前から伝わるか
- 一時変数でも、分岐や変換の意図を隠していないか

### 初回調査の重点対象
- `js/components/Keycap.js`
  - 表示用テキスト、icon 判定、縮尺計算まわりの一時変数が多く、意味の近い値が並んでいるため
- `js/components/Keyboard.js`
  - key 配列生成、split 判定、scale 計算など、座標系の一時変数が多いため
- `js/components/DeviceSlot.js`
  - UI 状態・表示制御・設定更新のローカル変数が多く、責務の見通し改善余地が大きいため
- 必要に応じて `js/utils/` 配下の pure logic へ対象を広げる

### 進め方
1. 対象ファイルを読み、誤読しやすい変数名を「ローカル変数」「引数名」「状態名」「外部仕様名」に分類する
2. 変更候補ごとに、単なる内部 rename で済むものと、参照元追跡が必要なものを切り分ける
3. 外部仕様に触れない安全な rename から優先して実施する
4. 近接する変数同士で意味の粒度をそろえる
5. rename 後に読みやすさが下がる箇所がないか、周辺の分岐名・補助変数名もまとめて見直す

### 実装ステップ
1. 重点対象ファイルごとに、候補変数名と変更理由を洗い出す
2. 影響範囲の小さい rename から順に適用する
3. export 名や保存データキーに波及する候補は別枠で扱い、必要なら今回スコープ外へ退避する
4. 変更後に `npm run lint` と `npm run test` を実行し、命名変更に伴う参照漏れを確認する
5. 必要に応じて `node --check` も使い、対象ファイル単位で素早く構文確認する

### 確認ポイント
- rename が挙動変更を起こしていないこと
- 似た責務の値が、より区別しやすい名前になっていること
- boolean 名が条件分岐の文章として自然に読めること
- 一時変数の命名改善によって、逆に冗長になりすぎていないこと
- 外部仕様名と内部都合の rename を混同していないこと

### 実施メモ
- `k`, `val`, `i`, `mK` のような短い名前は、使用範囲が狭くても周辺に近い概念が多い箇所では優先的に再検討する
- 一方で、ループのごく局所的な index や座標計算の慣用的な短縮は、可読性とのバランスを見て残す
- 変更候補が多い場合は、一度に全域へ広げず、まずは `Keycap` / `Keyboard` / `DeviceSlot` の 3 ファイルで改善効果を確認する
- 第二弾では `js/utils/labelParser.js` と `js/components/keycapIconUtils.js` を優先し、表示ロジックの分岐で意味が埋もれやすい短縮名を重点的に整理する
- 続く対象として、`js/components/DeviceSlot.js` の設定 UI 周辺と `js/app.js` の状態復元・読込フロー・ドラッグ処理周辺も同じ基準で見直す
- 最終仕上げとして、未着手ファイルに残っていた既存 lint warning も、未使用変数・未使用引数の整理で解消する
