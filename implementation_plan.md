# 実装計画

## 実装メモ

- 2026-05-28 着手:
  - 既存の WebHID / 定義読込の先行実装は土台として活かす
  - ただし入口は `MAPPING` モーダルから外し、`LOAD` 導線へ一本化する
  - `ファイルから読み込む` は `LAYOUT` 先行、その直後に `MAPPING` 追加読込確認を出す
  - `デバイスに接続して現在の情報を読む` は `キャッシュ → 現在スロット → registry → その場で LAYOUT 読込` の順で解決する
  - 今回要件と干渉する「定義JSONを直接選ばせる既存入口」は無効化し、`LAYOUT` 用読込として再配置する
- 2026-05-28 実装反映:
  - `DeviceSlot` の `LAYOUT` / `MAPPING` ボタンを `LOAD` ボタンへ置換
  - `LoadModal` を新設し、初期 2 択 / `LAYOUT` 後の `MAPPING` 確認 / 接続失敗後の `LAYOUT` 再誘導を段階表示にした
  - `loadJsonUtils.js` で `LAYOUT` / `MAPPING` JSON 判定を共通化し、`LAYOUT` 場面での誤 `MAPPING` 選択メッセージを追加
  - WebHID 読込時は `ローカルキャッシュ → 現在スロットの LAYOUT → registry → ユーザ指定 LAYOUT` の順で解決し、成功時はブラウザローカルへ自動保存するよう変更
- 2026-05-28 追加修正:
  - `現在スロットの LAYOUT` を再利用する際、接続実機の VID/PID と一致する場合だけ採用するよう修正する
  - 接続失敗後にユーザが選ぶ `LAYOUT` 用 JSON についても、接続実機との VID/PID 一致を必須にする
- 2026-05-28 キャッシュ再修正:
  - 誤ったレイアウトを別 VID/PID で保存しないよう、キャッシュ保存時に定義側の VID/PID を上書きしない
  - 接続実機の `productName` とキャッシュ済み定義名が明らかに矛盾する場合は、そのキャッシュを破棄して後続解決へ進める
- 2026-05-28 スロット再修正:
  - 過去不具合で汚染された `current slot design` を再利用しないよう、現在スロット分岐にも `productName` と定義名の整合チェックを入れる
- 2026-05-28 ヘルプ更新:
  - `HOW TO USE` を現行 `LOAD` 導線に合わせ、`ファイルから読み込む` / `WebHID` / ブラウザキャッシュの説明へ更新する

## 目的

- 既存の `LAYOUT` / `MAPPING` ボタンを廃止し、読込導線を `LOAD` ボタンへ集約する
- 本サービスの責務を「ユーザが用意した `LAYOUT` の上に、`MAPPING` を読み込んで表示すること」に整理する
- 初回接続デバイスは `LAYOUT` 用 `.json` の補助を受けながら現在の `MAPPING` を読めるようにする
- 過去に同一ブラウザで読込成功したデバイスは、ブラウザ内キャッシュを使って一発でレイアウトとマッピングを表示できるようにする

## 要件

### 1. 既存の `LAYOUT` / `MAPPING` ボタンは廃止する

- `DeviceSlot` 上の既存 `LAYOUT` ボタンと `MAPPING` ボタンを撤去する
- 代わりに `LOAD` ボタンを新設し、読込の入口を一本化する

### 2. `LOAD` ボタン押下時に読込モーダルを表示する

- `LOAD` 押下時の最初のモーダルには、次の 2 つだけを表示する
  - `ファイルから読み込む`
  - `デバイスに接続して現在の情報を読む`
- このモーダルは、手段の違いをユーザに明確に伝えつつ、選択肢を増やしすぎないことを優先する

### 3. `ファイルから読み込む` 導線

- まず `LAYOUT` 用 `.json` を読み込む
- 読込成功後に、続けて `MAPPING` 用 `.json` を読み込むかを尋ねる
- 選択肢は少なくとも次を持つ
  - `読み込む`
  - `後で`
- `MAPPING` 用 `.json` を後で読み込む場合でも、`LAYOUT` だけ表示された状態で作業を続けられるようにする
- ユーザが `LAYOUT` 用読込の場面で誤って `MAPPING` 用 `.json` を選んだと判断できる場合は、公式配布の定義 `.json` など `LAYOUT` 用ファイルを用意するよう案内する

### 4. `デバイスに接続して現在の情報を読む` 導線

- WebHID でユーザにデバイス選択をさせる
- 接続後は次の優先順で `LAYOUT` 解決を試みる
  1. ブラウザ内キャッシュに保存済みのレイアウト情報
  2. 現在スロットに表示中の `LAYOUT`
  3. サービスが既知として持つ最小限の registry
  4. ユーザがその場で読み込む `LAYOUT` 用 `.json`
- `LAYOUT` を解決できた場合は、その上に実機から読んだ現在の `MAPPING` を重ねて表示する

### 5. デバイス接続時の失敗導線

- 初回接続などで `LAYOUT` を解決できず情報取得に失敗した場合は、エラーメッセージを表示する
- その後、自動的または同一モーダル内で、`LAYOUT` 用 `.json` を読み込ませるプロンプトを表示する
- ここでいう失敗には少なくとも次を含む
  - ブラウザ内キャッシュが存在しない
  - 現在スロットに `LAYOUT` が無い
  - registry に該当情報が無い
  - ユーザがその場で `LAYOUT` を与えない

### 6. ブラウザキャッシュ

- 同一ブラウザで一度読込に成功したデバイスについては、レイアウト情報と必要な関連情報をブラウザ内へ保存する
- 次回以降は、`デバイスに接続して現在の情報を読む` を選ぶだけで、一発でレイアウトとマッピングを表示できる状態を目指す
- このキャッシュはユーザ端末ローカルに留め、サービスの配布物や共有資産へ自動反映しない

## 基本方針

### サービスの責務

- サービスの主責務は `MAPPING` の読込と表示である
- `LAYOUT` は、基本的にユーザが公式配布物などの適切な `.json` を与える前提を維持する
- ただし UX 向上のため、過去に成功した `LAYOUT` はブラウザ内で再利用してよい

### 読込の考え方

- `ファイルから読み込む`
  - `LAYOUT` を先に確保し、その後必要なら `MAPPING` を読む
- `デバイスに接続して現在の情報を読む`
  - まず `LAYOUT` を解決し、その後に WebHID で現在の `MAPPING` を読む
- どちらの導線でも、最終的には `design` + `keymapJson` の形へ寄せて既存描画系に流し込む

### キャッシュの考え方

- 保存対象は、同一デバイスを次回識別して再利用するためのローカル情報とする
- 少なくとも `vendorId` / `productId` / `layout definition` / `savedAt` を持てるようにする
- 必要であれば将来的に `name` や補助メタ情報を追加する
- このキャッシュはライセンス責任をサービス側で負うための仕組みではなく、あくまでユーザ端末ローカルの利便性向上と位置付ける

## UI 方針

### `LOAD` モーダル

- 最初のモーダルでは説明を増やしすぎず、次の 2 択に絞る
  - `ファイルから読み込む`
  - `デバイスに接続して現在の情報を読む`
- 補助文言として
  - 初回接続時は `LAYOUT` 用 `.json` が必要になること
  - 過去に同じブラウザで成功したデバイスは一発表示できること
  を短く添える

### `LAYOUT` 読込後の追加入力

- `LAYOUT` 読込成功後は、同じ流れの中で `MAPPING` 用 `.json` の読込確認を出す
- ユーザの作業を止めすぎないため、`後で` を必ず用意する

### 接続失敗時の導線

- 失敗時は単にエラーで終わらせず、次の行動を提示する
- 最低限
  - `LAYOUT` 用 `.json` を読み込む
  - `閉じる`
  を用意する
- 将来的に必要であれば `現在のスロットの LAYOUT を使って再試行する` も検討余地とする

## 想定変更対象

- `js/components/DeviceSlot.js`
  - `LAYOUT` / `MAPPING` ボタン廃止
  - `LOAD` ボタン新設
- `js/components/Modals/`
  - 新しい `LOAD` 導線モーダル
  - `LAYOUT` 読込後の `MAPPING` 追加読込確認 UI
  - 接続失敗時の再誘導 UI
- `js/app.js`
  - `LOAD` 起点の読込状態管理
  - `LAYOUT` 読込後の連続導線
  - WebHID 接続時の `LAYOUT` 解決順序の実装
  - ブラウザキャッシュの保存・再利用制御
- `js/utils/hid/`
  - キャッシュ済みレイアウトの解決 helper
  - 必要なら registry 参照 helper
- `js/utils/`
  - `LAYOUT` / `MAPPING` 用 `.json` の判定と整形 helper

## 実装順案

1. `implementation_plan.md` の本要件を前提に、既存 `LOAD` 導線へ置き換える設計を固定する
2. `DeviceSlot` から `LAYOUT` / `MAPPING` ボタンを外し、`LOAD` ボタンを追加する
3. `LOAD` 押下時の最初の 2 択モーダルを実装する
4. `ファイルから読み込む` 導線で
   - `LAYOUT` 読込
   - 続く `MAPPING` 読込確認
   を実装する
5. `デバイスに接続して現在の情報を読む` 導線で
   - WebHID 接続
   - キャッシュ確認
   - 現在スロットの `LAYOUT` 利用
   - registry 利用
   - 最後に `LAYOUT` 用 `.json` 読込へフォールバック
   を実装する
6. 読込成功したデバイスのレイアウト情報をブラウザへ保存する
7. 同一ブラウザでの再接続時に、一発でレイアウトとマッピングを表示できることを確認する
8. 失敗時のメッセージと次アクションを調整する

## 検証観点

- `LAYOUT` / `MAPPING` ボタンが消え、`LOAD` ボタンに置き換わっている
- `LOAD` モーダルの最初の選択肢が 2 つに整理されている
- `ファイルから読み込む` で
  - `LAYOUT` が読める
  - 続けて `MAPPING` を読むか尋ねられる
  - `後で` を選んでも `LAYOUT` 表示が維持される
- 初回接続デバイスで `デバイスに接続して現在の情報を読む` を選ぶと、`LAYOUT` 不足時に適切なエラーと次導線が出る
- `LAYOUT` を与えた後に、同じ接続操作で現在の `MAPPING` を表示できる
- 同じブラウザで二度目以降は、接続だけでレイアウトとマッピングを一発表示できる
- ブラウザキャッシュを消した場合は、再び初回導線に戻る

## 質問・確認事項

### 1. `MAPPING` 用 `.json` の読込確認は任意でよいか

- 現時点では `読み込む / 後で` の 2 択を想定している
- これにより、`LAYOUT` だけ先に確保したいケースを阻害しない

### 1.5 `LAYOUT` 用読込時の誤ファイル検出

- `layouts` や `matrix` が無く、代わりに `layers` が主である場合など、`MAPPING` 用 `.json` と判断できるケースでは専用メッセージを出す
- メッセージでは
  - いま必要なのは `LAYOUT` 用 `.json` であること
  - 公式配布の定義 `.json` などを用意してほしいこと
  を明示する

### 2. 接続失敗後の導線は自動遷移か、モーダル内の選択か

- UX と実装の単純さのバランス次第で決める
- 現時点では「エラーを出した上で、同一モーダル内に次アクションを出す」方式が自然と考える

### 3. 既存スロットに `LAYOUT` がある場合の扱い

- 今回の思想では、ここを積極的に活かして `MAPPING` だけ差し替えるべき
- ただし `matrix` や `layouts` の整合が明らかに崩れる場合の警告は将来検討余地がある

## 次エージェント向けメモ

- ここでは計画の再定義のみを行い、途中実装の是非はこの文書に持ち込まない
- 今回の基準は `LOAD` 集約、`LAYOUT` はユーザ主導、`MAPPING` はサービス主導、ブラウザキャッシュで一発復元、の 4 点
- 実装時は、既存の `design` + `keymapJson` 前提を活かしつつ、読込導線だけを整理し直す
