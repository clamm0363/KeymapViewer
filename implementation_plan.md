# 実装計画

## 目的

Fluent 表示モードのキーキャップ表現を、既存の「中央に主表示、下部に補足表示」という文法に揃えたまま、レイヤ系タイプ表示と修飾キー表示の役割を整理する。

あわせて、表示確認用のサンプルとして `SampleLayouts/sample_numpad.json` の `Layer 7` に今回の改修対象キーをまとめて配置し、実装中に見比べやすい状態を作る。

## 今回の対象

- `SampleLayouts/sample_numpad.json`
- `js/components/Keycap.js`
- `js/components/keycapLayerSection.js`
- `js/components/keycapModSection.js`
- 必要に応じて関連する共有スタイル/補助関数
  - `js/components/keycapStyles.js`
  - `js/components/keycapSections.js`
  - `js/components/keycapIconUtils.js`

## 現状把握

- レイヤキーは、中央に遷移先レイヤー、下部フッタ帯に `MO` などのタイプを表示している。
- 修飾キー系も、中央に主表示、下部帯に修飾名を表示しており、レイヤキーと視覚文法が近い。
- Fluent モード全体としては、中央に主役のアイコンや主表示、右下に控えめな補足情報を置くデザインが基調になっている。
- `LT` などの複合キーは、メイン領域内でオフセット表示する仕組みがすでにあり、これを活かして洗練する方針が適している。
- 表示検証用の `sample_numpad.json` の `Layer 7` は現在未使用で、今回の確認用サンプルをまとめる余地がある。

## 方針

- レイヤ系タイプ (`MO`, `TG`, `TT`, `OSL`, `TO`, `DF`, `LT` など) は、下部帯ではなく上部余白側へ移し、機能種別として見せる。
- 修飾キー情報 (`ALT`, `CTRL`, `HYPR`, `MEH` など) は、Fluent モードの補足情報として下部側に寄せる。
- 中央の主表示は、可能な限り「押した結果」や「主役のキー内容」を優先する。
- `LT` / `MT` / `*_T(...)` など複数機能キーは、既存のオフセット表示を活かしつつ、短押し/長押しの関係が見やすい構成へ整理する。
- まず `Layer 7` に代表例を配置し、実装後の見比べ対象を先に固定する。

## Layer 7 に置く確認用キー

- レイヤ系単機能
  - `MO(1)`
  - `TG(2)`
  - `TT(3)`
  - `OSL(4)`
  - `TO(5)`
  - `DF(6)`
- レイヤ系複合
  - `LT(1,KC_SPC)`
  - `LT(2,KC_TAB)`
- 修飾キー系
  - `LALT(KC_Z)`
  - `LCTL(KC_ESC)`
  - `LSFT(KC_A)`
  - `HYPR(KC_BSPC)`
  - `MEH(KC_ENT)`
- Mod-Tap 系
  - `MT(MOD_LGUI,KC_SPC)`
  - `LGUI_T(KC_TAB)`
  - `RALT_T(KC_DEL)`

## 実施手順

1. `sample_numpad.json` の `Layer 7` を今回の確認用キーで更新する。
2. 現行のレイヤキー/修飾キー描画ロジックを読み、上部ラベル・下部補足・中央主表示の責務分担を整理する。
3. レイヤキー表示を、下部帯中心から上部タイプ表示中心へ再設計する。
4. 修飾キー表示を、下部補足情報としてより一貫した見た目へ寄せる。
5. `LT` / `MT` 系のオフセット表示を、上部/下部の新ルールと衝突しないよう調整する。
6. 必要なら共有スタイルを整理し、重複を減らす。
7. 静的検証または軽量実行検証を行う。

## 実施済み

- [x] 作業ブランチ `feat/layer7-design-samples` を作成した。
- [x] `sample_numpad.json` の既存レイヤ構成を確認した。
- [x] `parseKeyLabel` とレイヤ/修飾キー描画コードを確認し、今回使う代表キーが概ね現行パーサで扱えることを確認した。
- [x] `Layer 7` の確認用マッピングを更新した。
- [x] レイヤキー表示の新しい視覚ルールをコードへ反映した。
- [x] 修飾キーおよび複合キー表示を新ルールへ揃えた。
- [x] 変更した描画ファイルの構文チェックを行った。
- [ ] ブラウザ確認で見つかった、中央揃え・上部タグ位置・オフセット表示の崩れを修正する。
- [ ] ブラウザ確認で見つかった、上部タグと右上サブ表示のサイズ過多を修正する。
- [ ] 上部タグの角丸領域を廃止し、ブラウザの最小フォントサイズ影響も踏まえて軽量表示へ再調整する。
- [ ] ブラウザ確認を踏まえ、上部ラベルを左上寄せに変更し、右上サブ表示を少し下げる。
- [ ] 上部ラベルをキートップ左上へさらに詰め、オフセット配置の再調整は一旦保留する。
- [ ] 上部ラベルの縮小基準点を左上へ変更し、左上へ寄り切らない要因を解消する。
- [ ] 上部ラベルの左右位置をわずかに戻しつつ、上方向の詰めを優先して再調整する。
- [ ] 上部ラベルの上方向余白をさらに削り、左位置は維持したまま微調整する。
- [ ] 上部ラベルをさらに 2px 上へ詰めて最終位置を確認する。
- [ ] Mac スタイル時の修飾表示で、`OPT` / `CMD` などを文字ではなく記号ベースへ揃える。
- [ ] Mac スタイル + Fluent 表示時に、単一修飾の補助表示を既存 SVG へ置き換える。
- [ ] `MEH` / `HYPR` など固有名を持つ複合修飾は、SVG 化せずテキスト表示を維持する。
- [ ] オフセット表示のサイズ・位置・Mac 記号の見え方を再調整する。
- [ ] オフセット表示の主表示と副表示の相対位置を保ったまま、全体を右上へ少し持ち上げて重なりを避ける。
- [ ] 左下主表示もテキストとアイコン系で同じ基準ボックスへ載せ、表示位置差を抑える。
- [ ] `FN_MO13` / `FN_MO23` など複数 FN 入力キーの右下表示を、`L1+L3` ではなく `FN1+FN3` のような実入力表記へ揃える。
- [ ] オフセット以外のレイヤーキー/修飾キー主表示に残る個別サイズ上書きを外し、通常キーと同じテキストサイズ規則へ揃える。
- [ ] テキスト系キー表示で小文字が混ざらないよう、オフセット主表示も含めて大文字化ルールを統一する。
- [ ] 左上ラベルと右下表示のサイズバランス、およびオフセット主表示の文字サイズを再調整する。
- [ ] 左上ラベルと右下テキストを同一の中間サイズへ揃え直す。
- [ ] 右下単一修飾 SVG のはみ出しを抑え、右下テキスト相当の視覚サイズへ揃える。
- [ ] 右上 `CMD` SVG の潰れを避けるため、元アイコン形状と描画サイズを再確認する。
- [ ] `DEL` を含む 3 文字オフセット主表示を、通常 3 文字よりわずかに小さく調整する。

## これからやること

- [ ] 上部タグをキートップ中央へ安定配置する。
- [ ] 中央主表示の垂直・水平センタリングを整える。
- [ ] 左下・右上のオフセット表示をブラウザ差に強い座標とサイズへ調整する。
- [ ] 上部タグと右上サブ表示を一段コンパクトに調整する。
- [ ] 上部タグをテキストのみの軽い表現へ変更し、必要ならオフセット主表示のサイズも再調整する。
- [ ] 上部ラベルと右上サブ表示の最終位置を微調整する。
- [ ] 上部ラベルの左上位置を優先して詰める。
- [ ] 上部ラベルの文字ボックス由来の余白を減らす。
- [ ] 上部ラベルの最終位置を数px単位で詰める。
- [ ] Mac スタイルの修飾ラベル表現を既存原則へ揃える。
- [ ] 単一修飾 SVG と複合修飾テキストの切り分けが視覚的に破綻しないか確認する。
- [ ] オフセット表示の右上サブと左下主表示のバランスを詰める。
- [ ] 左上ラベル・右下表示・右下アイコンの相対サイズを揃える。
- [ ] 左上ラベルと右下テキストのサイズ指定を同値で見直す。
- [ ] 軽量な検証を行い、表示ロジックの破綻がないか確認する。

## 完了条件

- `Layer 7` を開くと、今回の改修対象キーを一通り比較できる。
- レイヤ系タイプが上部、修飾情報が下部、主表示が中央という文法に揃う。
- `LT` / `MT` 系でも、短押し/長押しの関係が現状より読み取りやすい。
- 既存の Fluent モード全体の印象を崩さず、統一感が上がる。

## 注意点

- `sample_numpad.json` は表示確認用なので、機能種別の網羅性を優先する。
- レイヤ系と修飾系で、同じ見た目の帯を流用しすぎない。
- 既存の Text モードや他カテゴリ表示を不必要に巻き込まない。
