# 実装計画

## 目的

`.json` に記述されたキーコードを、アプリ側で独自のキーコードへ作り替えることなく表示する。
表示判定・アイコン解決・ラベル解決の全経路を見直し、QMK/VIA 公式キーコードと公式エイリアスだけで完結する状態へ寄せる。

## ブランチ

- `feature/qmk-via-official-keycodes`

## 今回の対象

- `js/utils/labelParser.js`
- `js/utils/helpers.js`
- `js/keymap-dictionary.js`
- `js/components/Keycap.js`
- `js/components/keycapIconUtils.js`
- 必要に応じて `js/components/keycapEncoder.js`

## 今回の非対象

- SVG アイコンの新規追加
- キーキャップ見た目の全面的なデザイン変更
- JSON データファイル自体の書き換え

## 現状認識

- 文字列キーコードの一部が表示前に `KC_` 付きへ補完されており、入力値そのものと表示判定用の値がずれている。
- `keymap-dictionary.js` に公式エイリアスとプロジェクト独自エイリアスが混在している。
- 未定義キーでも `deriveTextFromKeycode()` により表示文言が合成されるため、公式定義にない独自表示が発生しうる。
- `helpers.js` の `getRawLabel()` は文字列入力から `KC_` を外し、数値入力も短縮形へ変換しているため、元のキーコード表記を保持していない。
- サンプルやマップ済み JSON には `KC_MS_UP`、`QK_CLEAR_EEPROM`、`TRNS`、`RGB_MOD` など公式キーコードまたは公式エイリアスが混在している。

## 改善方針

- 入力キーコードはまず「そのまま保持」し、表示用の lookup 時だけ公式エイリアス解決を行う。
- エイリアス解決は `公式エイリアス` と `独自エイリアス` を分離し、表示系では前者だけを使う。
- 未知のキーコードは独自の正式コードへ変換せず、入力値をそのまま表示フォールバックに使う。
- カテゴリ判定や SVG 判定は、元文字列ではなく「公式に解決済みの lookup key」を参照する。
- 既存サンプルで使われている `TRNS`、`QK_CLEAR_EEPROM`、`KC_MS_UP` などの公式表記が崩れないことを優先する。

## 実施手順

1. 公式キーコード解決の責務を `keymap-dictionary.js` に集約する。
2. 公式エイリアス群と独自エイリアス群を分離し、表示ロジックが前者のみ参照するようにする。
3. `getRawLabel()` / `parseKeyLabel()` を見直し、入力キーコードの原表記を保持する。
4. `buildDisplayRaw()` などの独自補完を整理し、lookup 用キーと表示用キーを分離する。
5. アイコン判定・カテゴリ判定・下ラベル判定を公式解決済みキー基準へ置き換える。
6. エンコーダーの tooltip など周辺表示も同じ解決経路へ合わせる。
7. 静的検証を行い、代表的な公式キーコードと公式エイリアスの表示を確認する。

## 確認観点

- `KC_NO` / `KC_NONE` が `Text` モードでも空表示になること
- ツールチップ内のキーコードが正規のキーコード表記へ統一されること
- `KC_MS_UP` / `KC_MS_LEFT` / `KC_MS_BTN1`
- `QK_CLEAR_EEPROM`
- `TRNS` / `KC_TRNS`
- `RGB_MOD` / `KC_RGB_MOD`
- `KC_VOLU` / `KC_MNXT`
- `LT(1,KC_SPC)` / `MT(MOD_LGUI,KC_SPC)` / `LSFT(KC_A)`
- 未登録キーコードのフォールバック表示

## 完了条件

- 表示ロジックがプロジェクト独自キーコード生成に依存していない。
- 公式キーコードと公式エイリアスから、同じ表示結果へ一貫して到達できる。
- 未知の値に対しても、独自変換ではなく入力値ベースで安全に表示できる。
- 関連ファイルの静的検証が通る。
